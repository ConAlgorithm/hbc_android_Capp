<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff"
    >

    <include
        android:id="@+id/cancel_reason_titlebar"
        layout="@layout/include_header" />

    <TextView
        android:id="@+id/cancel_reason_confirm_tv"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:gravity="center"
        android:textSize="15sp"
        android:text="提交"
        android:textColor="#ffffff"
        android:background="#d2d2d2"
        />
    <android.support.v7.widget.RecyclerView
        android:id="@+id/cancel_reason_recyclerview"
        android:layout_below="@id/cancel_reason_titlebar"
        android:layout_above="@id/cancel_reason_confirm_tv"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</RelativeLayout>