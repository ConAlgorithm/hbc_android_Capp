<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/search_city_item_parent"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:background="@drawable/search_city_name_bg">

    <ImageView
        android:id="@+id/search_city_item_tag"
        android:layout_width="3dp"
        android:layout_height="18dp"
        android:layout_centerVertical="true"
        android:background="#E98100"
        android:visibility="gone" />

    <TextView
        android:id="@+id/city_name"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="20dp"
        android:layout_toLeftOf="@+id/has_sub_img"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:lines="1"
        android:textColor="@drawable/search_city_name_text_color"
        android:textSize="14sp" />

    <ImageView
        android:id="@+id/has_sub_img"
        android:layout_width="5dp"
        android:layout_height="9dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="20dp"
        android:gravity="center_vertical"
        android:src="@mipmap/ente_icon"
        android:visibility="gone" />

</RelativeLayout>